# Local state written by bx runs (locks, recorded machine shapes, artifacts).
local/
# Staged package roots and install prefixed builds.
staging/
# Local agent state (pi session/auth/models) — never part of the repo.
.pi/
